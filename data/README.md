# Data Directory

Place Lending Club raw loan files in `data/raw/`.

Raw files are gitignored because they are large and may have redistribution restrictions.

Write cleaned, filtered, and feature-engineered datasets to `data/processed/`.
