<!--
Committed, not generated at build time. This is a real card from a real run:

    riskscore make-sample-data data/sample/loans.csv --rows 12000 --seed 7
    riskscore train data/sample/loans.csv --no-cache
    riskscore card

Synthetic data, so the numbers describe the generator rather than the credit
market - which is the point of committing it: `reports/` is gitignored, so
without this file the shape of a real card would be documented only in prose.
Regenerate with the three commands above whenever a section changes.
-->

# Model card: `20260824T193816649Z-logistic_regression-origination_only-2bb6ca1`

Generated by `riskscore` from `metrics.json` and `manifest.json` in this run directory.
Every number below was measured by the run that wrote those files. Nothing here is maintained by hand.

## At a glance

| Field | Value |
| --- | --- |
| run id | `20260824T193816649Z-logistic_regression-origination_only-2bb6ca1` |
| created (UTC) | 2026-08-24T19:38:16.649Z |
| model | `logistic_regression` |
| feature tier | `origination_only` |
| git commit | `2bb6ca1` |
| extract | `data/sample/loans.csv` (sha256 `cbd647a2cd1ef56b`, 3.6 MB) |
| rows train / validation / test | 3818 / 1024 / 1619 |
| decision threshold | 0.2100 (selected on validation) |
| AUC (test) | 0.6793 |
| Brier (test, calibrated) | 0.1101 |
| calibration error (test) | 0.0240 |
| approval rate (test) | 0.8653 |

## Intended use

Screening applications for the product this extract describes: unsecured
consumer instalment loans, scored **at origination** from what an applicant
states plus a credit bureau pull.

The output is a calibrated probability of default over the full term and one
approve/decline recommendation against the published threshold. It is decision
*support*. The threshold encodes one cost ratio, stated below, and a lender whose
costs differ should reselect it on their own validation data rather than inherit
this one.

## Out of scope

- **Pricing.** This model estimates risk; turning a risk into a rate is a
  separate decision with separate regulation.
- **Servicing and collections.** Only origination-time information is used, so
  the model knows nothing about how an existing loan has been paid.
- **Any other product or population.** A different term, a secured product, or a
  population unlike the training window is out of scope by construction, and the
  drift table is how that is detected rather than assumed.
- **Unreviewed adverse action.** The reason codes are exact and are meant to be
  read by a person before a decline is communicated.

## Training data

**Target.** 1 = ['charged off', 'default', 'does not meet the credit policy. status:charged off']; 0 = ['does not meet the credit policy. status:fully paid', 'fully paid']; any other status is excluded rather than treated as repaid

**Rows at each stage.** A run that discarded 40% of its input must not look like one that discarded none.

| Stage | Rows |
| --- | --- |
| raw | 12000 |
| closed | 9326 |
| mature | 7289 |
| in_scope_terms | 6461 |
| labelled | 6461 |
| train | 3818 |
| validation | 1024 |
| test | 1619 |

**Time partitions.** The split is by origination date, and only the training partition was fitted on.

| Partition | Window |
| --- | --- |
| train | 2013-01-01..2014-09-30 |
| validation | 2014-10-01..2015-03-31 |
| test | 2015-04-01..2015-12-31 |

**Outcome-maturity embargo.** Filtering to closed loans alone keeps a young loan only when it *defaulted*, so the measured default rate climbs with vintage for a reason that is arithmetic rather than credit. Loans whose full term had not elapsed by the snapshot are removed: embargo snapshot=2018-12-01 kept=7289/9326 immature=2037 unknown_maturity=0

| Vintage | Default rate before | After |
| --- | --- | --- |
| 2013 | 0.162 | 0.162 |
| 2014 | 0.175 | 0.145 |
| 2015 | 0.188 | 0.130 |
| 2016 | 0.328 | - |

**Term filter.** [36] months. Downstream of the embargo rather than an independent choice: 60-month loans survive the embargo only in the earliest vintages, so admitting them trains on a term mix that validation never sees.

## Features

This run **excludes** the lender-priced tier (int_rate, grade, sub_grade, installment), which is the default. Those columns encode the lender's own risk assessment, so a model using them cannot score an applicant who has not been priced yet. See ADR 0005; `comparison.json` quantifies what excluding them costs.

`features=20 (numeric=15 categorical=5) engineered=6 inputs=20 lender_priced=off`

| Feature | Kind | Meaning |
| --- | --- | --- |
| `loan_amnt` | numeric | Requested principal in dollars. |
| `term` | numeric | Loan term in months. Also drives the outcome-maturity embargo. |
| `annual_inc` | numeric | Self-reported annual income. |
| `emp_length` | numeric | Years at current employer, capped at 10 by the source encoding. |
| `revol_bal` | numeric | Total revolving balance. |
| `delinq_2yrs` | numeric | 30+ day delinquencies in the last two years. |
| `inq_last_6mths` | numeric | Credit inquiries in the last six months, excluding auto and mortgage. |
| `open_acc` | numeric | Open credit lines. |
| `total_acc` | numeric | Total credit lines ever opened. |
| `pub_rec` | numeric | Derogatory public records. |
| `dti_clean` | numeric | Debt-to-income parsed to a float and bounded to a plausible range. |
| `credit_utilization` | numeric | Revolving utilization as a fraction. Falls back to total_credit_utilized / total_credit_limit when revol_util is absent. |
| `loan_to_income_ratio` | numeric | Requested principal divided by annual income. |
| `credit_history_months` | numeric | Months between first credit line and origination. Replaces a raw date string that would otherwise be one-hot encoded into 655 columns. |
| `fico_midpoint` | numeric | Midpoint of the origination FICO band. Absent from both real extracts. |
| `purpose` | categorical | Borrower-stated use of funds. |
| `home_ownership` | categorical | RENT, MORTGAGE, OWN, or OTHER. |
| `verification_status` | categorical | Whether the platform verified stated income. |
| `addr_state` | categorical | Two-letter state of residence. |
| `fico_band` | categorical | Interpretable FICO bucket, for reporting rather than accuracy. |

**Leakage audit.** admitted=18 refused=5 unclassified=0 lender_priced=off. The audit is a record, not a filter: what keeps a post-origination column out of the model is that the feature spec never contains one.

## Metrics

Measured on the test window, scored once, with the threshold and the calibrator frozen beforehand.

| Metric | Value | What it is |
| --- | --- | --- |
| AUC ROC | 0.6793 | ranking, all thresholds |
| average precision | 0.2547 | ranking, weighted towards the defaults |
| KS statistic | 0.2996 | largest separation between the two score distributions |
| Brier score | 0.1101 | calibrated probability accuracy, lower is better |
| Brier score (uncalibrated) | 0.1109 | the same model before the correction |
| calibration error | 0.0240 | mean gap between stated and observed default rate |
| calibration error (validation, in-sample) | 0.0155 | the floor, not a result: fitted on these rows |
| default rate | 0.1334 | the base rate of the test window |
| approval rate | 0.8653 | share approved at the threshold |

**Calibration.** `sigmoid`, fitted on validation and applied to every score this bundle produces. Measured *and* applied: a reported calibration curve whose correction is thrown away describes a model nobody would ship.

**Decision rule.** Approve when the calibrated probability is below 0.2100. Chosen on validation by minimizing expected cost with a funded default costed at 5.0 against 1.0 for a declined good loan, a ratio of 5.0:1. Validation cost at the selected threshold: 635.0.

## Performance by origination year

One AUC over a multi-year window hides whether the model works in every year of it.

| partition | vintage | rows | defaults | default_rate | auc_roc | brier_score | approval_rate |
| --- | --- | --- | --- | --- | --- | --- | --- |
| train | 2013 | 2161 | 331 | 0.153 | 0.714 | 0.121 | 0.870 |
| train | 2014 | 1657 | 232 | 0.140 | 0.727 | 0.112 | 0.891 |
| validation | 2014 | 508 | 81 | 0.159 | 0.639 | 0.129 | 0.888 |
| validation | 2015 | 516 | 61 | 0.118 | 0.626 | 0.102 | 0.884 |
| test | 2015 | 1619 | 216 | 0.133 | 0.679 | 0.110 | 0.865 |

## Population stability

Population Stability Index, train against test. Bands are the conventional 0.1 and 0.25. PSI needs no labels, which is why it is the number to watch between now and the next outcome.

| Measure | PSI | Band |
| --- | --- | --- |
| score | 0.0084 | stable |
| worst feature (`credit_history_months`) | 0.0298 | see `psi_features.csv` |

No feature is outside the stable band.

## What the model uses

Ranked by mean absolute SHAP contribution on the test window, computed by the `linear` path. Full table in `shap_summary.csv`; per-applicant reason codes come from the same values, so a global ranking and an individual explanation cannot disagree.

| Rank | Feature | Meaning |
| --- | --- | --- |
| 1 | `credit_utilization` | Revolving utilization as a fraction. Falls back to total_credit_utilized / total_credit_limit when revol_util is absent. |
| 2 | `fico_midpoint` | Midpoint of the origination FICO band. Absent from both real extracts. |
| 3 | `emp_length` | Years at current employer, capped at 10 by the source encoding. |
| 4 | `credit_history_months` | Months between first credit line and origination. Replaces a raw date string that would otherwise be one-hot encoded into 655 columns. |
| 5 | `dti_clean` | Debt-to-income parsed to a float and bounded to a plausible range. |

## Limitations

- One time-ordered split and no cross-validation, so every number here carries the sampling noise of a single test window.
- Test was scored once. Any change made after reading these numbers makes them optimistic, and there is no second held-out window left.
- The calibrator was fitted on validation, so the validation calibration error is in-sample. It is the floor the test number should be read against, not a second result.
- Labels come from the extract's own status column at the snapshot date. A charge-off recorded after the snapshot reads as repaid.
- Reason codes assume feature independence (interventional SHAP) and are in uncalibrated log-odds. Calibration is monotone, so it moves the probability without reordering the reasons.
- A high PSI says a feature moved, not that the model got worse. Read it beside the SHAP summary: a feature the model barely uses can drift hard and change nothing.
- The test window is 1619 rows. Read every metric above as a statement about that many loans.

## Ethical considerations

**No protected attribute is a feature.** Race, colour, religion, national
origin, sex, marital status and age appear nowhere in this extract, so the model
cannot use them directly. That is not the same as fairness, and it is important
not to read it as such: in the United States, geography correlates with race, and
`addr_state` is a feature here.

**No fairness metric is reported, because none can be computed from these
columns.** Saying so is more useful than substituting a proxy and calling the
question answered. A deployment subject to ECOA needs a disparate-impact test
against attributes this dataset does not contain, on data that does.

**The explanations are exact, not indicative.** Each reason code is a SHAP value
in the model's own log-odds, and the full set sums to the score, so a reviewer
can check a decline rather than take it on trust.

## Reproducing this run

| Component | Version |
| --- | --- |
| bundle schema | 1 |
| python | 3.14.5 |
| numpy | 2.4.6 |
| pandas | 3.0.5 |
| scikit-learn | 1.9.0 |
| scipy | 1.18.0 |
| xgboost | 3.3.0 |
| joblib | 1.5.3 |
